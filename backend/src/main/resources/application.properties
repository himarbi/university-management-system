spring.application.name=backend
server.port=8080

# PostgreSQL Configuration (Default)
spring.datasource.url=jdbc:postgresql://localhost:5432/university_db
spring.datasource.username=postgres
spring.datasource.password=Himarbi_58
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA / Hibernate Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT properties
university.app.jwtSecret=universityManagementSecretKeyForAuthenticationJWTNeedsToBeLongEnoughForHS256Algorithm
university.app.jwtExpirationMs=86400000
